<form method="post" action="{{ path('order_product_delete', {'orderProduct': order_product.id, 'shop': shop.id, 'shopOrder': shop_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_product.id) }}">
    <button class="btn">Delete</button>
</form>
