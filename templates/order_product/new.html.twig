{% extends 'bootstrap.html.twig' %}

{% block title %}New OrderProduct{% endblock %}

{% block body %}
    <section class="content">
        <div class="row">
            <div class="col-md-offset-2 col-md-8 col-lg-offset-3 col-lg-6">
                <div class="box box-primary">
                    <div class="box-header with-border">
                        <h3 class="box-title">Add New Product</h3>
                    </div>
                    <div class="box-body">
                        {{ include('order_product/_form.html.twig') }}
                    </div>
                </div>
            </div>
            <a href="{{ path('order_product_index', {'shop' : shop.id, 'shopOrder' : shop_order.id}) }}"
               class="btn bg-navy margin pull-right">Back</a>
        </div>
    </section>
{% endblock %}
