<form method="post" action="{{ path('shop_order_delete', {'order': shop_order.id, 'shop': shop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shop_order.id) }}">
    <button class="btn">Delete</button>
</form>
